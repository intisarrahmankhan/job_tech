# JobScout scraper dependencies.
#
# `argparse` and `json` ship with the Python standard library — they
# are NOT pip-installed.  Pinning `argparse==1.4.0` (as previous
# revisions did) forces a stale shim onto modern Pythons; we drop it.
playwright==1.47.0
httpx==0.27.2
python-dotenv==1.0.1
